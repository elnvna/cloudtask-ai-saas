services:
  backend:
    build:
      context: ./backend

    container_name: cloudtask_backend

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend:/app
      - ./backend/uploads:/app/uploads

    restart: unless-stopped
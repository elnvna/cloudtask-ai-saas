apiVersion: v1
kind: Service

metadata:
  name: cloudtask-service
  namespace: cloudtask

spec:

  selector:
    app: cloudtask-backend

  ports:

  - protocol: TCP
    port: 8000
    targetPort: 8000

  type: NodePort